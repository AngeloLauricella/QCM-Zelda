<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Test CSRF</title>
</head>
<body>
    <h1>Test CSRF</h1>

    {% if message %}
        <p>{{ message }}</p>
    {% endif %}

    <form method="POST">
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('form_test') }}">
        <button type="submit">Envoyer</button>
    </form>
</body>
</html>
