{% extends "base.html.twig" %}

{% block title %}Statistiques - QCM Zelda{% endblock %}

{% block content %}
<div class="results-container">
    <h2>Vos Statistiques</h2>

    <div class="stats-summary">
        <div class="stat-card">
            <h3>Score Total</h3>
            <p class="big-number">{{ stats.score }}</p>
        </div>
        <div class="stat-card">
            <h3>Réponses Correctes</h3>
            <p class="big-number">{{ stats.correctAnswers }}</p>
        </div>
        <div class="stat-card">
            <h3>Réponses Incorrectes</h3>
            <p class="big-number">{{ stats.wrongAnswers }}</p>
        </div>
        <div class="stat-card">
            <h3>Pourcentage de Réussite</h3>
            <p class="big-number">{{ stats.percentage }}%</p>
        </div>
    </div>

    <div class="results-by-category">
        <h3>Résultats par Catégorie</h3>

        <div class="category-results">
            <h4>🌟 Introduction</h4>
            {% if introResults %}
            <table class="results-table">
                <thead>
                    <tr>
                        <th>Question</th>
                        <th>Votre Réponse</th>
                        <th>Résultat</th>
                    </tr>
                </thead>
                <tbody>
                    {% for result in introResults %}
                    <tr class="{% if result.correct %}correct{% else %}incorrect{% endif %}">
                        <td>{{ result.question.title }}</td>
                        <td>{{ result.question.options[result.userAnswer] }}</td>
                        <td>{% if result.correct %}✅ Correct{% else %}❌ Incorrect{% endif %}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            {% else %}
            <p>Aucune réponse pour cette catégorie</p>
            {% endif %}
        </div>

        <div class="category-results">
            <h4>🌳 Forêt de Kokiri</h4>
            {% if foretResults %}
            <table class="results-table">
                <thead>
                    <tr>
                        <th>Question</th>
                        <th>Votre Réponse</th>
                        <th>Résultat</th>
                    </tr>
                </thead>
                <tbody>
                    {% for result in foretResults %}
                    <tr class="{% if result.correct %}correct{% else %}incorrect{% endif %}">
                        <td>{{ result.question.title }}</td>
                        <td>{{ result.question.options[result.userAnswer] }}</td>
                        <td>{% if result.correct %}✅ Correct{% else %}❌ Incorrect{% endif %}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            {% else %}
            <p>Aucune réponse pour cette catégorie</p>
            {% endif %}
        </div>

        <div class="category-results">
            <h4>⛰️ Montagne</h4>
            {% if montagneResults %}
            <table class="results-table">
                <thead>
                    <tr>
                        <th>Question</th>
                        <th>Votre Réponse</th>
                        <th>Résultat</th>
                    </tr>
                </thead>
                <tbody>
                    {% for result in montagneResults %}
                    <tr class="{% if result.correct %}correct{% else %}incorrect{% endif %}">
                        <td>{{ result.question.title }}</td>
                        <td>{{ result.question.options[result.userAnswer] }}</td>
                        <td>{% if result.correct %}✅ Correct{% else %}❌ Incorrect{% endif %}</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            {% else %}
            <p>Aucune réponse pour cette catégorie</p>
            {% endif %}
        </div>
    </div>

    <div class="actions">
        <a href="{{ path('app_home') }}" class="btn btn-primary">Retour à l'accueil</a>
        <a href="{{ path('results_leaderboard') }}" class="btn btn-secondary">Voir le Classement</a>
    </div>
</div>
{% endblock %}
